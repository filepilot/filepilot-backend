spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
logging.level.com.filepilot=DEBUG

# Local dev only: allow boot with default JWT secret / DB password.
# Any non-dev deployment must set JWT_SECRET (and DB_PASSWORD) env vars.
app.allow-dev-defaults=true
